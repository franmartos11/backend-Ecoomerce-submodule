server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/burgosfacundo/ConfigServer-PI2
          default-label: main
          force-pull: true